[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gitr"
version = "0.1.0.dev0"
description = "Lightweight Git diff viewer"
requires-python = ">=3.10"

[project.scripts]
gitr = "gitr:main"

[tool.setuptools]
py-modules = ["gitr"]
